"""Benchmark and evaluation scripts for reflection-agent-pattern.

Not part of the installed package — invoked directly (``python
scripts/benchmark_reflection_delta.py``) or imported by tests via the
``scripts`` pythonpath entry in pyproject.toml.
"""
